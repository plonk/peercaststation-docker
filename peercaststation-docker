#!/bin/sh

### BEGIN INIT INFO
# Provides:          peercaststation
# Required-Start:    $remote_fs $syslog
# Required-Stop:     $remote_fs $syslog
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: PeerCastStation
# Description:       PeerCastStation
### END INIT INFO

IMAGE=plonk/peercaststation:latest
SETTINGS_DIR=/etc/peercast

. /lib/lsb/init-functions

case "$1" in
    start)
	mkdir -p $SETTINGS_DIR
        docker run -d -i -P --log-driver=syslog --net=host -v /etc/peercaststation:/root/.config/PeerCastStation $IMAGE > /var/run/peercaststation.lock
        exit 0
        ;;
    stop)
        docker exec `cat /var/run/peercaststation.lock` mono /root/PeerCastStation-1.9.2/PeerCastStation.exe -kill
        exit 0
        ;;
    *)
        echo "Usage ${0##*/} {start|stop}" >&2
        exit 3
        ;;
esac

exit 0
